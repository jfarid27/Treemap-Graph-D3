var treemapGraphD3 = require('./../treemap-graph-d3.js')

describe('Default indexNodes', function(){
    var instance, indexer 
    beforeEach(function(){
        instance = treemapGraphD3({})
        indexer = instance.indexNodes()
    })

    describe('when given nodes', function(){

        it('should return result of reducer')

        describe('reducer', function(){
            it('should index node in nodeIndex')
            it('should return new nodeIndex')
        })

    })
})
